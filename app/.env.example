DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require
ADMIN_PASSWORD=change-this-admin-password
# Optional: set to local only when you want to bypass Neon and use the bundled JSON store.
# CONTENT_STORE_MODE=local
